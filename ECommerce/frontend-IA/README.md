Frontend (Vite + React)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Default dev server: `http://localhost:3000` (proxies to API at `/api` by default). Set `VITE_API_BASE` env var to your API base if needed.

Basic components included: Login, ProductList, Cart, Checkout.
